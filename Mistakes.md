## Mistakes Made

- Returned `nums` instead of `k` in LeetCode 27.
- Used `num.size()` instead of `nums.size()`.
- Initially focused on code before fully understanding the two-pointer pattern.
- Needed revision on the roles of read pointer and write pointer.