class Solution {
public:
    string sortVowels(string s) {

        vector<int> lower(26, 0);
        vector<int> upper(26, 0);

        // Count vowels and mark their positions
        for (int i = 0; i < s.size(); i++) {

            if (s[i] == 'a' || s[i] == 'e' || s[i] == 'i' ||
                s[i] == 'o' || s[i] == 'u') {

                lower[s[i] - 'a']++;
                s[i] = '#';
            }

            else if (s[i] == 'A' || s[i] == 'E' || s[i] == 'I' ||
                     s[i] == 'O' || s[i] == 'U') {

                upper[s[i] - 'A']++;
                s[i] = '#';
            }
        }

        // Build sorted vowel string
        string vowels = "";

        for (int i = 0; i < 26; i++) {
            while (upper[i] > 0) {
                vowels += char('A' + i);
                upper[i]--;
            }
        }

        for (int i = 0; i < 26; i++) {
            while (lower[i] > 0) {
                vowels += char('a' + i);
                lower[i]--;
            }
        }

        // Put vowels back
        int j = 0;

        for (int i = 0; i < s.size(); i++) {

            if (s[i] == '#') {
                s[i] = vowels[j];
                j++;
            }
        }

        return s;
    }
};