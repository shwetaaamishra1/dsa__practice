#include<iostream>
using namespace std;

int fact(int n)
{
    //base case
    if(n==0)
    return 1;

    return n*fact(n-1);

}
int main(){
    //factorial of n number

    int n;
    n=6;
    if(n<0)
    {
        cout<<"factorial is not possible";
        return 0;
    }
    cout<<fact(n);
}
