class Solution {
public:

void lpsfind(vector<int>&lps,string s ){
        // creation of variables 
        
        int pre=0,suf=1;

        while(suf<=s.size()-1){

        // if the characers at pre and suf is =

        if(s[pre]==s[suf]){
            lps[suf]=pre+1;
            pre++;
            suf++;
        }else{
        // if they are not equal either they can be zero or non zero
            if(pre==0)
            {
                lps[suf]=0;
                suf++;
            }
            else
            {
                pre=lps[pre-1];
                
            }
        }
        }
    }
    int strStr(string haystack, string needle) {
        vector<int>lps(needle.size(),0);
        lpsfind(lps,needle);
        int first=0,second=0;

        while(first<haystack.size()&& second<needle.size())
        {
            // match

            if(haystack[first]==needle[second])
            {
                first++,second++;
            }
            // not match
            else
            {
                if(second==0)
                first++;
                else
                second=lps[second-1];
            }
        }

        if(second==needle.size())
        return first-second;
        else
        return -1;
    }
};
